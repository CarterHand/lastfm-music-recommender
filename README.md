# lastfm-music-recommender
An applied, content-based music recommendation engine leveraging ensemble regression models (Random Forest, Gradient Boosting) and semantic tag similarity on the Last.fm Million Song Dataset.
